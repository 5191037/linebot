kind: pipeline
type: docker
name: default
steps:
  - name: test
    image: docker:dind
    volumes:
      - name: dockersock
        path: /var/run/docker.sock
    commands:
      - docker build -t hc-docker.sophia.local/matsubara/linebot:1.1
      - docker push hc-docker.sophia.local/matsubara/linebot:1.1
